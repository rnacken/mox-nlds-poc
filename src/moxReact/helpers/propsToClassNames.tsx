// Type to inlineSize, blockSize, padding, paddingInlineStart, etc
// Same for colors
// Border styles (?)

export const propsToClassNames = (
  props: Record<string, any>
): Array<string> => {
  return [];
};
