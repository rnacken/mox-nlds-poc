import { Meta } from "@storybook/addon-docs/blocks";

import Card from "./assets/textBox/card.png";

<Meta title="Waarom?" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Waarom?

    Uit het NLDS volgen we de belangrijkste aanbevelingen en regels op, maar we stappen ook bewust af van een aantal ideeen: we zien wat problemen zien bij de huidige aanpak van andere NLDS-gebaseerde systemen (bv Rijkshuisstijl Community - RHC):

    ## 1. 😕 Inconsistentie in kwaliteit / naamgeving / APIs / aanwezigheid / etc

    De huidige lijst met componenten van NLDS mist een hierarchie (complex en eenvoudig staan door elkaar) en consistentie, want een componenten komen uit Utrecht, RHC, Amsterdam, etc.

    Vaak zijn de componenten complex door veel mogelijke `props`, maar tegelijkertijd mis je ook vaak essentiele dingen, zoals een `as` prop om het `ElementType` van een component aan te passen.
    Een `NavLink` is bv een hardcoded `<a href>`, wat niet gaat werken als je een routing systeem gebruikt zoals `NextJS`.
    Verder zijn er complexe componenten waarbij CSS via `tokens` aangepast kan worden, maar lang niet alle styling is zo aanpasbaar.
    Componenten zijn niet te goed terug te brengen tot de basis bouwblokken, om een eigen implementatie te bouwen.

    ### Mox aanpak:
    - We zullen voor Mox *geen* componenten van andere implementaties gebruiken.
    - We hebben een atomische structuur van **Atoms, Molecules en Organisms** (van eenvoudig naar complex). De **Atoms** zijn de kleinste bouwblokken, waar alle **Molecules** uit opgebouwd zijn (en **Organisms** bestaan weer uit **Atoms** en **Molecules**).

    ### ➡️ Gevolgen:
    - Consistente **Atoms**, die je altijd zelf kunt gebruiken om een eigen **Molecule/Organism** component te maken.
    - **Atoms** doen een 'belofte': ze hebben altijd dezelfde API (altijd `as`, `ref`, `className` en `styling` props, nooit omringende spacing zoals `padding` of `margin`, altijd slechts 1 tag, zoals `div`).
    - Voor styling kunnen we gebruik maken van stabiele `@layer` opzet, zodat gebruikers makkelijk styling kunnen overriden.

    ---

    ## 2. 😕 Geen universeel scaling systeem voor spaces / layouts

    Er is `by design` voor gekozen om geen spacing-componenten te maken (bv een flex-box element). Dit is een probleem want daardoor zijn er nu componenten die:
    - Spacing properties (`gap`, `padding`, `width`, etc) hardcoded hebben (e.g. `2rem`), wat niet responsive meeschaalt met onze scaling.
    - Properties definieren die niet door een `token` aan te passen zijn. Zeker met complexe componenten zijn daardoor niet flexibel genoeg in gebruik.
    - Er zijn componenten die zelfs white-space rondom hebben, waardoor je deze niet goed kan gebruiken in layouts.

    ### Mox aanpak:
    - We definieren 1 scaling systeem, voor zowel typografie als spacing.
    - We zorgen voor een text-box trimming om ongewilde text-leading te voorkomen (zie [CSS: text-box](/docs/css-text-box--docs)).

    ---

    ## 3. 😕 Wildgroei aan tokens

    Momenteel heeft het Rijkshuisstijl Community theme bijna 1400 `tokens`, die allemaal aangepast kunnen worden in Figma (en de Tokens Studio plugin).
    Voor designers is dit niet goed meer bij te houden, en zodra er een update komt van de token-lijst is het lastig om te controleren wat de impact is op een design.

    ### Mox aanpak:

    - We houden enkel een lijst van `tokens` bij voor de Atoms in ons systeem: de kleinste bouwblokken.
    - Voor **Molecules & Organisms** bestaan er geen `tokens` (mogelijk een enkele uitzondering).

    ### Gevolgen:

    - Drastische vermindering van tokens, waardoor je als designer weer overzicht hebt in Figma.
    - Als je een **Molecule of Organism** component wilt aanpassen:
      - doe je dat via de `tokens` voor de **Atoms**,
      - of: bouw je je eigen component gebaseerd op de **Atoms**

  </div>
</div>
